namespace Server.Utiles
{
    public enum ObjectType
    {
        None,
        Player
    }
    public enum RoomState
    {
        Lobby,
        Prepare,
        InGame,
        Between,
        Bomb,
        GameEnd
    }
}
